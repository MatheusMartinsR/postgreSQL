create table t_aluno(
id serial,
nome varchar(255),
cpf char(11),
observacao text,
idade integer,
dinheiro numeric(10,2),
altura real,
ativo boolean,
data_nascimento date,
horario_aula time,
dt_matricula timestamp
);

select * from t_aluno;
